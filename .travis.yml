language: php
sudo: false
install:
  - mkdir -p build/logs
  - printf "\n" | pecl install imagick
  - echo "extension = redis.so" >> ~/.phpenv/versions/$(phpenv version-name)/etc/php.ini
  - composer install --no-interaction --no-scripts
  - composer require satooshi/php-coveralls:dev-master
  - cp -n .env.travis .env
  - npm install -g npm
  - npm install --no-progress
  - npm install --no-progress -g gulp
  - npm install --no-progress -g typings
  - gulp
  - php artisan key:generate
script:
  - ./vendor/bin/phpcs --standard=./tests/analysis/phpcs.xml app
  - ./vendor/bin/phpmd --strict app text ./tests/analysis/phpmd.xml
  - phpunit --testsuite unit --repeat 3
  - phpunit --coverage-clover build/logs/clover.xml
php:
  - '7.0'
after_success:
  - travis_retry php vendor/bin/coveralls
